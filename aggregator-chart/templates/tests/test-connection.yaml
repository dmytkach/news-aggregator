apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aggregator-chart.fullname" . }}-test-connection"
  labels:
    {{- include "aggregator-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aggregator-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
