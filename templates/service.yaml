apiVersion: v1
kind: Service
metadata:
  name: news-aggregator-service
  namespace: news-aggregator
spec:
  selector:
    app: news-aggregator
  ports:
    - protocol: TCP
      port: 443
      targetPort: 8443
  type: NodePort
